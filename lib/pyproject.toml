[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "lib"
version = "0.1.0"
dependencies = []

# Import name `lib` maps to this directory (modules live next to pyproject.toml).
[tool.setuptools]
package-dir = {"lib" = "."}
packages = ["lib"]
