apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ingestor-certs
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ingestor
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: ingestor
      app.kubernetes.io/part-of: kepserverlogger
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ingestor
        app.kubernetes.io/part-of: kepserverlogger
    spec:
      hostname: usprmsmovmp01
      initContainers:
        - name: wait-for-timescaledb
          image: timescale/timescaledb:latest-pg16
          imagePullPolicy: IfNotPresent
          command:
            - sh
            - -c
            - until pg_isready -h "$DB_HOST" -p "$DB_PORT" -U "$DB_USER"; do sleep 2; done
          envFrom:
            - configMapRef:
                name: kepserverlogger-env
            - secretRef:
                name: kepserverlogger-secret
      containers:
        - name: ingestor
          image: ingestors
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: kepserverlogger-env
            - secretRef:
                name: kepserverlogger-secret
          volumeMounts:
            - name: config
              mountPath: /app/ingestors/settings.json
              subPath: settings.json
              readOnly: true
            - name: config
              mountPath: /app/ingestors/tags/balcones_pcn_tags.csv
              subPath: balcones_pcn_tags.csv
              readOnly: true
            - name: config
              mountPath: /app/ingestors/tags/balcones_dmz_tags.csv
              subPath: balcones_dmz_tags.csv
              readOnly: true
            - name: servers
              mountPath: /app/ingestors/servers.json
              subPath: servers.json
              readOnly: true
            - name: certs
              mountPath: /app/ingestors/certs
            - name: logs
              mountPath: /app/ingestors/logs
      volumes:
        - name: config
          configMap:
            name: kepserverlogger-files
        - name: servers
          secret:
            secretName: kepserverlogger-servers
        - name: certs
          persistentVolumeClaim:
            claimName: ingestor-certs
        - name: logs
          emptyDir: {}
